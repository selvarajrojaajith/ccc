#include <stdio.h>
int main()
{
    int high;
    printf("Enter two numbers(intervals): ");
    scanf("%d %d", &low, &high);
    printf("Enter the prime number are:",high);
    {
    high==1;
    break;
    }
  return;
  }
       
